name: publish_data
on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'
  push:
publish_data:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Check out this repo
        uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

